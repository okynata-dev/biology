# Mint pipeline output (generated locally, may be uploaded to R2 instead of git)
pngs/
metadata/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Playwright
playwright-report/
test-results/

# Node (if ever added)
node_modules/

# Wrangler local mock state (R2/KV/D1 development emulation — should not be committed)
.wrangler/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Local preview/dev tools (Claude Code launch config etc.)
.claude/

# Secrets / local config
.env
.env.local
.dev.vars

# Local exports — wallets export from /admin or wrangler queries
allowlist.json
waitlist*.json
waitlist*.csv
bioms-waitlist-*.csv
bioms-admin-token.txt


# Atlas build output (uploaded to R2, not committed)
atlas-out/
loops/
posters/
meta/glass/
