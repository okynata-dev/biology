# Cloudflare Pages — redirects. Evaluated BEFORE static assets, so these
# win over any .html file left in the repo.
#
# Post-mint retirement (2026-06-10): partner-application and public-list
# surfaces are gone. Old links from tweets/DMs land on the homepage.
/partners       /   301
/partners.html  /   301
/list           /   301
/list.html      /   301

# /bioms is BACK as the Bioms wing since 2026-08-31: the root became the
# Plods homepage and the whole Bioms world (lab, activity, biominers) lives
# under /bioms. Do not redirect it.

# Luoms retired entirely 2026-07-20 — the second collection never minted
# (totalSupply 0) and the announcement got no traction, so every surface
# is gone: the collection page, the phase-two signup, the live renderer
# and the two-column gate that used to be the homepage. The Bioms landing
# is back at /. Old links from tweets and DMs land there instead of 404.
# Everything is recoverable from git if the project is ever revived.
/luoms          /   301
/luoms.html     /   301
/glass          /   301
/glass-500      /   301
/glass-500.html /   301
/reserve        /   301
/reserve.html   /   301
