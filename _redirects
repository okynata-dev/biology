# Cloudflare Pages — redirects. Evaluated BEFORE static assets, so these
# win over the .html files still in the repo (kept for history/rollback).
#
# Post-mint retirement (2026-06-10): partner-application and public-list
# surfaces are gone. Old links from tweets/DMs land on the homepage.
# /reserve came BACK 2026-07-08 as the Luoms phase-two signup page —
# only the .html variant redirects, to the canonical clean URL.
/reserve.html   /reserve   301
/partners       /   301
/partners.html  /   301
/list           /   301
/list.html      /   301

# Luoms — short alias for the second collection's page.
/glass          /luoms   301
