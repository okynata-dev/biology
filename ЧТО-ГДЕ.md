# Что где лежит

## Тебе

**`ПРОМО/`** — всё видео, одним местом.

| Папка | Что внутри |
|---|---|
| `трейлеры/` | 6 готовых трейлеров |
| `коллажи/` | 3 сетки: 3×3, 4×4, 8×8 |
| `клипы/` | исходные клипы, из них собраны трейлеры |
| `bioms-evolution.mp4` | ролик эволюции |

## Не трогать

Всё остальное в корне — **это и есть сайт**. Cloudflare Pages публикует эту
папку целиком при каждом пуше в `main`, поэтому если унести отсюда
`index.html`, `worker.js`, `_headers` или `_redirects`, сайт ляжет.

| Что | Зачем |
|---|---|
| `index.html`, `lab.html`, `activity.html`, `biominers.html` и прочие `.html` | страницы сайта |
| `worker.js`, `wrangler.toml` | API на api.thebioms.com, деплоится отдельно |
| `_headers`, `_redirects`, `_worker.js` | настройки Cloudflare |
| `design.css`, `specimen-engine.js`, `nav-wallet.*`, `biominers.js` | общий код страниц |
| `scripts/`, `scripts_*.sh`, `*.py` | сборка картинок и заливка на R2 |
| `.github/workflows/` | автоматика, дёргает те самые `scripts_*.sh` |

Скрипты в корне выглядят мусором, но на них ссылаются воркфлоу и `wrangler.toml`,
поэтому перенос их сломает. Оставлены намеренно.

## Моё

**`_claude/`** — рабочее, тебе не нужно, в гит не попадает.

| Папка | Что внутри |
|---|---|
| `бэкапы/` | дамп старого вейтлиста, 188 485 адресов до чистки |
| `сборка/` | локальные остатки генерации, всё уже на R2 |
| `скрипты/` | быстрые параллельные заливки на R2 |
| `черновики/` | наброски страниц |

## Соседняя папка

Коллекция **Biominers** живёт отдельно: `~/Desktop/biominers`.
Там генератор на 4 444 токена, там же `README.md` с командами.
